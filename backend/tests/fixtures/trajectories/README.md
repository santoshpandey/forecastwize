# Trajectory fixtures

Checked-in JSONL examples for reviewers and tests. Generated by
`app.evidence.logger`; not live runs.

| File | Shows |
|---|---|
| `successful_run.jsonl` | inspect → diagnose → completed decision |
| `verification_retry.jsonl` | VERIFY FAIL → retry → PASS → accept |
| `tool_failure.jsonl` | PROFILE tool error → failed FINALIZE |

Tool payloads are under `artifacts/`. Schema: `backend/app/evidence/trajectory.py`.
