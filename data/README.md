# Place original series files here. Preprocessing must never overwrite these files.
# Generated evaluation CSVs live in `evaluation/` (see that folder's README).
# HTTP uploads and run records live in `api/` (gitignored; created at runtime).
# See docs/forecasting-methodology.md.
